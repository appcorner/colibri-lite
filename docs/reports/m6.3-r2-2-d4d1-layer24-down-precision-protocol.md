# M6.3-R2.2-D4D1 Layer24 Down-Projection Precision Protocol

Purpose: determine whether the D4 miss is resolved by finer existing grouped-int8 precision on Layer24 `down`, without changing the local quality budget or introducing a new artifact family.

Contract:
`models/qwen3-30b-a3b/m6.3-r2-2-d4d1-layer24-down-precision-contract-v1.json`

Contract SHA-256:
`16c6015c68834c639a57e4a381217e4cac6d3977c1de89316df073266998fb2a`.

Fixtures: `short_english`, `short_thai`.

Contexts per fixture:
- canonical F32 prefix through Layer23;
- Layer0 group32 plus F32 Layers1-23.

At Layer24, gate/up are canonical F32. Evaluate `down` with group32, group16, and group8 from the same activated product and compare routed output against same-input/router canonical F32. Target candidates are diagnostic only and are not propagated.

Selection is frozen in `32 -> 16 -> 8` order. A group must remain at or below `0.001` in all four fixture/prefix contexts. If no group passes, Layer24 falls back to canonical F32.

There is no generation, timing, native-kernel change, artifact conversion, retry, threshold change, or D5 authorization in D4D1. Any selected group requires a separate held-out quality re-entry before further promotion.
