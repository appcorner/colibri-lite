# M6.3-R2.2-D5a Production Hybrid Artifact Result

D5a passes.

The frozen Layer24 hybrid artifact is `f32 gate | f32 up | group8 down`, 14,942,208 bytes per expert and 1,912,602,624 bytes for 128 experts. Artifact SHA-256: `d94d12cbea648e2f2911573c893f564254cbde526d60132c600ed24e88317ac2`.

The builder performs no requantization. It copies F32 gate/up from canonical Layer24 and packed group8 down from the frozen D2 artifact. A separate verifier checked all 128 experts and every hybrid byte against those sources with `byte_exact=true`.

The Rust direct-consumption reader represents F32 gate/up plus packed group8 down and has no F32 down buffer. Its synthetic tests pass 3/3 and Clippy passes with `-D warnings`.

D5b production-path quality is authorized. D5c performance/resource measurement remains blocked until D5b passes.
