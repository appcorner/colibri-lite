# M6.3-R2.2-D4D2 Group16 Hybrid Held-Out Quality Result

D4D2 closes `NO-GO`.

The frozen execution used the exact D4D2 contract, release binary, harness,
reference, Layer0 group32 artifact, and Layer24 group16 artifact. Preflight
passed. The native test exited `101` after `793.70s`; durable transport itself
completed normally and stderr contains a single quality assertion failure.

The first failure is `short_thai` Layer24 scalar-hybrid versus same-input/router
F32 routed-output max-abs `0.0012040138`, above the frozen `0.001` limit by
`20.40138%`. No evidence TSV was written because the harness stops at the first
failed quality assertion.

This invalidates the assumption that D4D1 prompt-only cross-fixture local
characterization is sufficient for propagated autoregressive state. Group16
passes the prompt-only contexts but does not satisfy the full two-token quality
trajectory.

No threshold is relaxed, no automatic retry is allowed, and no performance,
resource, D5, all-layer, or M6.4 claim is authorized.
