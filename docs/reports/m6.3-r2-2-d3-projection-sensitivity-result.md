# M6.3-R2.2-D3 Projection Sensitivity Result

Status: complete. Diagnostic only.

The frozen D3 matrix completed `28/28` rows in `269.30s` with native exit `0`,
empty stderr, exact router guards, finite candidate outputs, and bit-exact
all-packed seam controls.

## Layer 24

Only `down` packed alone passes both frozen contexts:

| Variant | Worst local max-abs | Gate |
| --- | ---: | --- |
| gate+up+down | 1.16819e-2 | FAIL |
| gate+up | 1.18062e-2 | FAIL |
| gate+down | 1.13200e-2 | FAIL |
| up+down | 1.57225e-3 | FAIL |
| gate | 1.14551e-2 | FAIL |
| up | 1.48445e-3 | FAIL |
| down | 4.88266e-4 | PASS |

Selected policy: F32 gate + F32 up + group32 down.
## Layer 47

No packed subset passes. The lowest-error variant is `up` alone, with worst
local max-abs `1.63116e-2`, still more than 16x the frozen `0.001` limit.
Layer 47 therefore selects canonical F32 fallback.

## Implication

The data now supports projection-aware precision at Layer 24 but not Layer 47.
The exact three-sentinel proposal for a separate quality re-entry is:

- Layer 0: all group32;
- Layer 24: only down group32, gate/up F32;
- Layer 47: all F32.

Modeled payload is `4,932,501,504` bytes versus `7,247,757,312` bytes all-F32,
a `31.944444%` modeled reduction across these three layers. This is not measured
runtime benefit.

Evidence SHA-256:
`592adc98df7bdd0b6a3fc4f3f82ca4e4a2002cffb4a73ab8df7a59cd6693452c`.
Result SHA-256:
`b7ff3aafae497c5ecc5e482662f8fc87ed79beaf513dc8bf41658f633ecf18a5`.

R2.2c performance remains blocked until a separately pre-registered held-out
quality re-entry for the hybrid policy passes.
