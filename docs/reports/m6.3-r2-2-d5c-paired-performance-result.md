# M6.3-R2.2-D5c Paired Performance/Resource Result

D5c closes `GO` under the frozen D5 contract.

The accepted v2 evidence contains 40 fresh process samples: two fixtures x two cache labels x five balanced pairs x two paths. Samples SHA-256 is `47ac917cb7b3886e4ff786639c832e28936f1d64fd6c4335e5819d3f924c8415`. Frozen validator result SHA-256 is `b6387a2975074a9f2fc164c04a1741fc6324d64c54fd1947a27fd4e023a730e3`.

The earlier ETW transport attempt is excluded from performance evidence and admitted zero samples. ADR 0116 documents why: inference, READY/GO, memory sampling, and logical file reads succeeded, but the current Windows trace does not expose the exact previously validated physical-I/O join. D5 did not pre-register physical SSD bytes as a decision metric, so v2 used the frozen D5-specific READY/GO process-memory collector and reran the matrix from scratch.

## Frozen decision results

| Fixture | Median wall speedup | Median prefill improvement | Median decode improvement |
| --- | ---: | ---: | ---: |
| short_english | 3.4380% | 3.7308% | 3.5994% |
| short_thai | 4.0296% | 4.0062% | 4.4219% |

Hybrid wall wins by cell:

- English / first-process-touch: `5/5`
- English / likely-warm: `5/5`
- Thai / first-process-touch: `4/5`
- Thai / likely-warm: `5/5`

All cells pass the frozen `>=4/5` rule. Both fixture medians exceed the frozen `>=1.0%` wall-speedup rule.

Worst median memory regression is `0.8145196989818504%`, below the `1.0%` gate. The largest working-set regression is `0.291970802919708%`; some cells show lower hybrid working set than F32.

Logical expert payload is reduced in every pair. English drops from `21,743,271,936` to `21,323,317,248` bytes (`419,954,688` bytes less per run). Thai drops from `14,495,514,624` to `14,215,544,832` bytes (`279,969,792` bytes less per run). No physical SSD-read reduction is claimed.

All four frozen validator gates are `true`:

1. median wall speedup in each fixture,
2. at least four hybrid wins in every five-pair cell,
3. median working-set/private-byte regression within one percent,
4. positive logical expert-byte reduction in every pair.

The validated D5 policy therefore has a measured production-like end-to-end benefit on the frozen bilingual workload while preserving the already accepted D5b quality evidence. This result authorizes only a separate `M6.3-R2.3` all-layer plan design/review. It does not authorize a 48-layer implementation or M6.4.
