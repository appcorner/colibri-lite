# M6.3-R2.2-D4 Hybrid Held-Out Quality Result

Status: **NO-GO**.

The frozen D4 execution completed with native test exit code `101` after `556.37s`. Durable transport completed normally; stderr records a quality assertion failure rather than wrapper or connector failure.

The first failing gate was `short_english` Layer24 scalar hybrid versus same-input/router canonical F32:

- observed local max-abs: `0.0010073595`;
- frozen limit: `0.001`;
- absolute excess: `0.0000073595`;
- ratio to limit: `1.0073595x`.

The harness therefore stopped before writing the final TSV. No later generation/top20/native-repeatability gate is claimed either PASS or FAIL.

## Decision

Do not loosen the threshold and do not retry D4. The D3 Layer24 `down`-only group32 choice was characterized only on `short_thai`; D4 demonstrates that it does not generalize unchanged to `short_english`.

The next experiment must be separately pre-registered and limited to Layer24 `down` precision across both held-out fixtures using the already frozen group32/group16/group8 artifacts. D5, historical R2.2c, all-layer rollout, and M6.4 remain blocked.

Result SHA-256: `47dbccb7406ee12fcca5b0a4b48790a749c54388fc5780f5dfbb6765dd012822`.
