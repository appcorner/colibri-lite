# M4.1-08 Unified Manifest Report

## Scope

Created one canonical root manifest for the complete unquantized F32
Qwen3-30B-A3B artifact. It binds the pinned source contract, validated runtime
dimensions, dense payload, all expert shards, and tokenizer assets without
changing any component contract or performing conversion/inference work.

## Canonical root

| Property | Value |
| --- | --- |
| Artifact format | `colibri-lite-model` version 1 |
| Root file | `model-manifest-v1.json` |
| Root bytes | 11,395 |
| Root SHA-256 | `f133d733612840ad691d637732d4ef2de1e0242c4bb1d92521b49dfcfb1b8cd2` |
| Model ID | `Qwen/Qwen3-30B-A3B` |
| Revision | `ad44e777bcd18fa416d9da3bd8f70d33ebb85d39` |
| Architecture/model type | `Qwen3MoeForCausalLM` / `qwen3_moe` |
| Dtypes | BF16 source, F32 storage, F32 compute |
| Endianness | Little |

All 57 file records have unique canonical relative paths. Auditing found zero
absolute paths, local roots, temporary paths, user/host names, or timestamps in
canonical content.

## Components

| Component | Files | Logical bytes | Key count/hash |
| --- | ---: | ---: | --- |
| Source provenance | 1 | 12,786 | Source manifest SHA-256 recorded in root |
| Dense | 2 | 6,164,520,354 | 435 tensors; payload `bfad7fdc...40893` |
| Experts | 49 | 115,967,247,918 | 6,144 experts; 48 shards |
| Tokenizer | 5 | 15,885,859 | 4 assets; 151,643 base tokens |
| Root manifest | 1 | 11,395 | `f133d733...b8cd2` |
| **Complete artifact** | **58** | **122,147,678,312** |  |

Component-manifest hashes:

- dense: `aff0f4c59a71b98590bd2a1efc4e3fd6a95d58794063ce4b1092acce1597bc54`
- experts: `9c581c6c46ecf830e7d0dd0e380d26b17803784f009b37ef2657ae34d06b2939`
- tokenizer: `a66c5b39331656b1a3befd2d695265f15bdc5f16226fbbf7794bfb5ae9220c5e`

The expert ordered-shard-set SHA-256 is
`b90d537f5c0c202b2bf5db0e74b8bf8b9ba9ea5378d788733d2bf9e11d36bf91`.
All 48 individual shard hashes and all four tokenizer file hashes are embedded
in the root manifest.

## Validated runtime contract

The root records hidden size 2,048; 48 layers; 32 attention heads; 4 KV heads;
explicit head dimension 128; query/KV widths 4,096/512; 128 experts with 8 per
token; expert intermediate width 768; base intermediate width 6,144; RMS epsilon
0.000001; RoPE theta 1,000,000; SiLU; normalized top-k probabilities; no
attention bias/dropout, RoPE scaling, sliding window, dense-only layers, or tied
embeddings.

Model maximum positions 40,960, tokenizer-declared maximum 131,072, and
caller-configured runtime session capacity remain separate.

## Validation evidence

Metadata mode passed after hashing 19,187,816 bytes across 9 files while
checking all 57 referenced files and component cross-references. Full mode
passed after hashing every required file: 122,147,678,312 bytes across 58 files
including the root manifest.

Synthetic tests reject:

- missing, renamed, truncated, and same-size corrupted files;
- hash and size mismatches;
- missing and duplicate expert shards;
- unsupported root/dense component versions;
- incompatible architecture and model type;
- unknown critical root fields;
- incomplete temporary output.

## Determinism and relocation

Two independent generations were byte-identical and produced the same root
hash. The complete real artifact directory was moved to another root; metadata
validation still passed, and generation from the relocated directory produced
the same canonical bytes/hash. Synthetic full validation also passes after a
directory move.

## Dependency and scope review

The generator/validator uses only the Python standard library. No Cargo or
Python dependency, native library, unsafe boundary, or public API was added.
No model conversion, inference, tokenizer algorithm, quantization, mmap, SIMD,
GPU work, or optimization was performed.

## Reproduction

```powershell
python python\reference\model_artifact_manifest.py generate `
  <artifact-root> <output-manifest>
python python\reference\model_artifact_manifest.py validate-metadata `
  <artifact-root>
python python\reference\model_artifact_manifest.py validate-full `
  <artifact-root>
python -m unittest python\reference\test_model_artifact_manifest.py -v
```

## Verification

Canonical generation, metadata validation, full validation, relocation, and all
four grouped synthetic tests pass. `cargo fmt --all --check`,
`cargo check --workspace`, `cargo test --workspace`,
`cargo clippy --workspace --all-targets -- -D warnings`, and
`cargo run -p clr-cli` all pass. The workspace suite contains 118 tests with
zero failures or ignored tests; Clippy emits no warnings and the CLI reports
`bootstrap ready`.
