# M6.3-R2.2-D1 quality-drift localization protocol

Contract SHA-256:
`6f08cdaf5c0d297f887d00c2bd8cccfb6096885772bc8e464aaba9e0eeebdb9b`.

Purpose: explain the valid R2.2b `short_thai` scalar-group32 top-20 reorder
without changing artifacts, thresholds, or performance authorization.

Run one prompt-only scalar diagnostic for each frozen state, in order:

1. `[]` canonical F32 control
2. `[0]`
3. `[24]`
4. `[47]`
5. `[0,24]`
6. `[0,47]`
7. `[24,47]`
8. `[0,24,47]`

Selected sentinels use the frozen group32 artifact; every other layer stays F32.
No generation, timing, retry, or results-based subset selection is allowed.
For every state record prompt top-20 order/set, argmax, Layers 0/24/47 router
guards, frozen fixed/top-20 logit max-abs, and logits/ranks for tokens `94482`,
`69440`, `52388`, and `129075`. For each active sentinel also record the local
scalar-group32 versus same-input/router F32 routed-output max-abs.

Interpret the minimal-cardinality failing subsets only:

- singleton failure => that layer is sufficient;
- all singletons pass but pair/triple fails => accumulation/interaction;
- pair fails while both singleton members pass => pair interaction.

The F32 control must reproduce frozen semantics. D1 cannot authorize R2.2c,
change the R2.2 quality gate, requantize artifacts, or select a new candidate.
A separate hypothesis review follows D1.
