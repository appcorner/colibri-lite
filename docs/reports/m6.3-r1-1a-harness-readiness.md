# M6.3-R1.1a Harness Readiness

## Outcome

The pre-candidate R1.1a harness is ready to execute the pre-registered
group-64 and group-32 candidates on frozen `code_newline`. No candidate was
converted or executed while closing this gate, no admission outcome exists,
and R1.2 remains blocked.

The authoritative runtime changes only the Layer-0 expert source. Attention,
the router, layers 1--47, norms, KV cache, final norm, and LM head reuse the
canonical F32 loop. The candidate reader consumes packed INT8 values and F32
scales directly and reports zero complete F32 weight materializations.

## Evidence

- ADR 0064 calibration: two independent canonical F32 processes passed all
  exact gates and produced deterministic checkpoint SHA-256
  `a9ba90e1af1622d8308a6355f4f30808806f31fa1bfc94eec652b402704ada11`.
- Final calibration comparator: exit `0`, `1 passed`; persisted logs and hashes
  validate against the calibration record.
- Candidate reader: 4 focused tests passed for canonical sizes, direct packed
  consumption, hash/size validation, expert bounds, forbidden `-128`, and
  malformed payload rejection.
- Candidate telemetry validator: 5 tests passed, including rejection of the
  stopped group-128 candidate, artifact-path escape, F32 materialization, PID
  mismatch, and missing correlated physical I/O.
- Persisted runner: 3 tests passed for success, child exit `7`, and launch
  failure exit `127`, with non-null atomic exit records and verified log hashes.
- Post-reader canonical control run
  `m6.3-r1.1a-post-reader-control-20260801T143840Z`: exit `0`, `1 passed`,
  test duration `207.23s`; stdout SHA-256
  `53a09e697e1c45e0135178b020cb3420fdde4289e7724037b905723b3d3380f6`,
  stderr SHA-256
  `acbdf9703d04f66580b61992009760eaf9615e42fc57679d590f5a8d413a06c2`.
- `cargo fmt --all --check`, focused check/tests/Clippy, Python validators, and
  `git diff --check` passed before this readiness record.

## Next bounded execution

Create a new flat temporary run only after disk and read-only canonical-input
preflight. Reconvert group-64 and group-32 independently, validate exact known
hashes, then run three candidate processes per layout with the ADR 0065
candidate-correlated telemetry record. Characterize only `code_newline`, apply
the fixed ADR 0059 ranking and `0.05` cap, and update the append-only admission
record. Do not run held-out bilingual fixtures or begin R1.2.
