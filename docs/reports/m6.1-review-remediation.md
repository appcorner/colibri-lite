# M6.1 review remediation

## Resolved finding

The M6.1 review found that profile-model accepted the doctor-only
--rust-version option. The option was ignored, which weakened the invalid-input
contract.

Commit 36ddfbb now adds --rust-version to the accepted option set only for
doctor. The existing profile command regression test now asserts that
profile-model rejects the option as unknown.

## Reverification

The complete M6.1 verification passed after the fix:

- cargo fmt --all --check
- cargo check --workspace
- cargo test --workspace: 138 tests
- cargo clippy --workspace --all-targets -- -D warnings
- cargo run -p clr-cli
- Python reference tests: 83 tests
- Draft 2020-12 validation of both tracked profiles

M6.1 is accepted. The next task remains M6.2-01.
