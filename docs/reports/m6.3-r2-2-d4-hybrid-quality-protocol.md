# M6.3-R2.2-D4 Hybrid Held-Out Quality Protocol

Purpose: re-enter held-out quality with the exact projection-aware policy
selected prospectively by D3. This is not a performance benchmark.

Contract:
`models/qwen3-30b-a3b/m6.3-r2-2-d4-hybrid-quality-contract-v1.json`

Contract SHA-256:
`5f2b27b623a0456ad15e14b542816e4d5ad17bf32dacd91099184adfc86fb4fe`.

Candidate policy:
- Layer 0: group32 gate/up/down;
- Layer 24: F32 gate/up + group32 down;
- Layer 47: F32 gate/up/down;
- all other layers: canonical F32.

For each held-out fixture, execute scalar hybrid once and native hybrid twice.
Native group32 is used only where the policy is packed. Require exact generation,
prompt top-20, argmax, guard routers, finite logits, and the existing compact
F32 logit envelope.
Local gates:
- scalar hybrid versus same-input/router F32 routed max-abs <= `0.001` at
  Layers 0 and 24;
- native versus scalar hybrid routed max-abs <= `0.001` at Layers 0 and 24;
- native versus scalar prompt-logit max-abs <= `0.002`.

The two native runs must be exactly repeatable for generation, top-20, argmax,
router guards, prompt-logit hash, final-norm hash, and local error maps.

No automatic retry or post-result policy/threshold change is allowed. A PASS
only authorizes D5 hybrid artifact/layout and performance-contract design. The
historical R2.2c 108-sample contract remains inapplicable to this new candidate.
