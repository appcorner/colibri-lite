# M6.3-R2.2-D4D4 Group8 Hybrid Held-Out Quality Result

D4D4 completed successfully with exit code `0`, runtime `767.61s`, zero stderr, and evidence SHA-256 `a330462130baa7b06b52122ce5851ffe412ff6496ade909b418c370d04767a6b`.

The tested policy is Layer0 group32 gate/up/down; Layer24 canonical F32 gate/up plus group8 down; Layer47 and all remaining layers canonical F32. Native AVX2+FMA is used only at Layer0 in the mixed runs; Layer24 group8 remains scalar.

Both held-out fixtures preserve exact two-token generation, prompt top-20 ordering, prompt argmax, and Layers0/24/47 router guards. All logits are finite and the two mixed runs are exactly repeatable.

Layer24 scalar-hybrid/F32 max-abs is `0.0006817728` English and `0.0006819293` Thai, both below the frozen `0.001` limit. Layer0 mixed/scalar max-abs is `2.3841858e-7` for both fixtures. Mixed/scalar prompt-logit max-abs is `2.2888184e-5` English and `3.3378601e-5` Thai, both below the frozen `0.002` limit.

D4D4 is therefore `GO` for quality re-entry. This result authorizes only D5 hybrid artifact/layout and backend/performance-contract design. It is not a runtime throughput, resource, I/O, native-group8, all-layer, or M6.4 claim.
