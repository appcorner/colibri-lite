# M6.3-R1.1c Verified M4 Pinned-Source Recovery

## Outcome

The original verified M4 Safetensors subset was recovered offline as canonical
minimal oracle source. A same-volume atomic directory rename moved it from the
temporary M4 root to the registry-bound stable root; no 61 GB copy and no
separate source deletion occurred.

## Validation

- Closed allowlist: 17 files, no child directories or unexpected files.
- All 17 byte lengths and SHA-256 hashes matched
  `source-manifest-v1.json`; total was 61,068,275,406 bytes.
- The parsed index has 18,867 tensor mappings and references exactly the 16
  present shards, with no unsafe path or unreferenced shard.
- Identity is `Qwen/Qwen3-30B-A3B`, revision
  `ad44e777bcd18fa416d9da3bd8f70d33ebb85d39`, Apache-2.0,
  `Qwen3MoeForCausalLM`.
- The missing nine metadata/tokenizer files are explicitly absent; no file was
  fabricated. This is not a full 26-file Transformers snapshot.

## Offline proof and promotion

Offline selective loading resolved embedding, Layer-0 norms/attention/router,
and Layer-0 expert 0 gate/up/down tensors directly from the indexed shards.
The frozen M4.2 Transformers 5.12.1 tooling instantiated Qwen3-MoE RMSNorm,
attention, router, and MLP components after checking the pinned config contract.
No candidate or Rust output supplied oracle values and no checkpoint was made.

The stable root is the only approved absolute path and is recorded in
`minimal-oracle-source-registry-v1.json`. Post-move rehash passed. ACL checks
showed the current identity and `CodexSandboxUsers` only have ReadAndExecute;
Administrators and SYSTEM retain FullControl.

## TLS status

The TLS chain issue remains unresolved for network/full-snapshot work. It is
non-blocking for selective R1.1b oracle use because this source is already
verified, local, and read-only.

## Next

M6.3-R1.1b — freeze code_newline routed-expert and MoE checkpoints.
