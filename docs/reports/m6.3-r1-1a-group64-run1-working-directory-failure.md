# M6.3-R1.1a Group-64 Run 1 Working-Directory Failure

## Status

Infrastructure failure before checkpoint comparison. This run is not candidate
numerical evidence and cannot contribute to admission or ranking.

## Evidence

Fresh group-64 conversion produced 641,728,512 bytes with SHA-256
`35a3ef6aba723d302fb1a7fded6ede4543a0c3dfcd35651596b78f1c5158cad2`.
The required candidate environment bindings passed fail-fast validation. ETW
launched child PID 27500 and collected 1,715 memory samples while the full-model
workload ran for approximately 195 seconds.

The process exited 101 before checkpoint comparison because the collector
forced the repository root as its working directory. The test executable's
frozen R1.1b path `../../models/...` must instead resolve from
`crates/clr-qwen3-moe`. Physical I/O remained `not_measured`; no candidate
checkpoint or logit result was produced.

## Cleanup

The failed run was not reused. Reviewed cleanup dry-run and apply removed
exactly 21 files and 654,667,024 logical bytes. The filesystem reported
654,696,448 reclaimed bytes, the canonical registry remained valid, and the
run directory no longer exists.

## Correction

ADR 0066 requires an explicit in-repository `working_directory`, records it in
the capture result, and adds a non-elevated `-ValidateConfigOnly` preflight.
The regression suite covers valid, missing, outside-repository, and missing-
environment configurations. A fresh group-64 conversion and three fresh
correlated executions remain required. Group-32 and R1.2 remain blocked.
