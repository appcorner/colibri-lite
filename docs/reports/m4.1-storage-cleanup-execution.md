# M4.1 Stable Artifact Promotion and Cleanup Execution

## Outcome

The canonical Qwen3-30B-A3B artifact was promoted by a same-volume directory
rename, without copying payload bytes, from:

```text
D:\tmp\colibri-m4.1-08-moved\relocated-model-artifact-v1
```

to the stable registered root:

```text
D:\models\colibri-lite\qwen3-30b-a3b\artifact-v1
```

The old artifact path no longer exists. The stable root contains 58 files and
122,147,678,312 logical bytes. Its root-manifest SHA-256 remains
`f133d733612840ad691d637732d4ef2de1e0242c4bb1d92521b49dfcfb1b8cd2`.

`models/qwen3-30b-a3b/canonical-root-registry-v1.json` binds this stable path,
root hash, manifest size, model identity/revision, and 58-file count. Cleanup
now requires that registry and refuses canonical roots within the temporary
namespace or paths that differ from the registry.

## Pre-cleanup validation

- Metadata validation passed at the stable root: 57 required component files,
  9 hashed metadata files, and 19,187,816 hashed bytes.
- Full-integrity validation passed across all 58 files and
  122,147,678,312 bytes.
- The dense payload and each of the 48 expert shards had two hard links before
  cleanup: one stable canonical name and one approved completed-task name.
- The second registry-bound dry-run listed exactly the 11 approved candidates,
  containing 117 files.
- The 17 pinned source files under `D:\tmp\colibri-m4.1-05` were individually
  protected and were not candidates.

## Cleanup accounting

| Measurement | Bytes |
| --- | ---: |
| Disk free immediately before deletion | 202,957,639,680 |
| Disk free immediately after deletion | 325,221,920,768 |
| Logical bytes removed | 244,392,722,124 |
| Expected last-link reclaim from reviewed inventory | 122,264,231,628 |
| Shared hard-link namespace removed without freeing canonical allocation | 122,128,490,496 |
| Actual physical bytes reclaimed from disk measurements | 122,264,281,088 |

The actual physical reclaim is the measured free-space delta. It is 49,460
bytes greater than the sum of last-link file lengths because NTFS allocation
and filesystem metadata are not represented by logical file lengths.

Deletion was limited to the exact 11 paths in
`docs/reports/m4.1-storage-cleanup-plan-v1.json`. All 11 are absent afterward.
The stable canonical root and all pinned source files remain present. The
canonical dense payload and 48 expert shards each have one remaining link.

## Post-cleanup validation

- Canonical metadata validation passed with root hash
  `f133d733612840ad691d637732d4ef2de1e0242c4bb1d92521b49dfcfb1b8cd2`.
- Canonical full-integrity validation passed across all 58 files and
  122,147,678,312 bytes.
- Pinned-source validation passed for 17 files and 61,068,275,406 bytes at
  revision `ad44e777bcd18fa416d9da3bd8f70d33ebb85d39`.
- All 6 cleanup safety tests passed, including canonical-registry mismatch
  refusal.
- All 3 pinned-source validator tests passed.
- `cargo fmt --all --check` passed.
- `cargo check --workspace` passed.
- `cargo test --workspace` passed: 118 passed, 0 failed, 0 ignored.
- `cargo clippy --workspace --all-targets -- -D warnings` passed.
- `cargo run -p clr-cli` passed with `x86_64-windows` and `bootstrap ready`.

No candidate outside the reviewed set was deleted. The pinned Safetensors
source remains in the temporary namespace because M4.2-01 requires it.

## Next task

M4.2-01 - validate selected tensor values against Safetensors.
