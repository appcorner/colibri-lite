# M6.3-R1.1a Execution Blocker

## Status

The pre-candidate harness is ready, but candidate execution is blocked by the
absence of an elevated Windows Administrator token required for process/path-
correlated Kernel-File and Kernel-Disk ETW. No candidate numerical result was
produced and the append-only admission outcome remains unchanged.

## Attempt and boundary

The canonical Layer-0 F32 shard passed size, SHA-256, read-only attribute, and
ACL preflight. Disk preflight passed with approximately 273.8 GB free against
the group-64 requirement of approximately 1.716 GB including the 1 GiB safety
reserve and retained 330,811-byte R1.1b checkpoint.

Group-64 was converted inside one flat temporary run. The promoted temporary
artifact was 641,728,512 bytes with SHA-256
`35a3ef6aba723d302fb1a7fded6ede4543a0c3dfcd35651596b78f1c5158cad2`,
matching the pre-registered historical conversion evidence. Conversion was
not treated as characterization or admission evidence.

The ETW collector stopped before launching the candidate process with:

```text
capture_m6_3_r1_etw.ps1 requires an elevated Administrator PowerShell
```

Therefore no candidate checkpoint, logits, process memory, or physical-I/O
record exists. Group-32 conversion was not started.

## Cleanup

Because debug retention was not authorized before the run, the reviewed
cleanup plan was run in dry-run mode and then apply mode. It removed exactly
five files and 641,730,472 logical bytes from the group-64 run; the filesystem
reported 641,732,608 reclaimed bytes. The cleanup validator rechecked the
canonical-root registry and protected Layer-0 source, and the run directory no
longer exists.

## Required unblock

Resume this task from an elevated Administrator Codex/PowerShell process. Run
the same read-only/hash/disk preflight, reconvert group-64 into a new unique
flat run, and invoke `scripts/capture_m6_3_r1_etw.ps1` with the committed fixed-
flat-run configuration. Only after one correlated capture succeeds should the
remaining independent runs and group-32 execution proceed. Do not weaken the
ADR 0065 physical-I/O gate or use held-out fixtures.
