# M6.3-R2.2-D4D3 Sequence-Aware Layer24 Down Precision Protocol

Purpose: measure whether Layer24 down group32/group16/group8 remains within the
frozen local budget after candidate state is propagated across the two-token
reference trajectory. This is diagnostic only.

Contract:
`models/qwen3-30b-a3b/m6.3-r2-2-d4d3-sequence-aware-down-precision-contract-v1.json`

Contract SHA-256:
`dddc8b4ec320835686e5c5286b420e5b39bb4f3bbb3ef95e0f6b82bab473470a`.

Run short_english and short_thai separately for each existing group32/16/8
Layer24 artifact. Layer0 is scalar group32; Layer24 gate/up are F32 and down is
the candidate group. Candidate hidden state propagates across positions, while
the token sequence is fixed to the frozen reference trajectory.

At each Layer24 position compute a same-input/router F32 comparator and record
local max-abs, router IDs, candidate finiteness, and frozen greedy agreement.
Select the first group in 32->16->8 order that stays <=0.001 for every position
of both fixtures; otherwise select F32 fallback.

No timing, native change, retry, new artifact, new precision, or quality-pass
claim is in scope. A selection can authorize only a separate quality re-entry.
