# M4.1-07 Offline Tokenizer Report

## Scope

Included the canonical tokenizer assets from `Qwen/Qwen3-30B-A3B` revision
`ad44e777bcd18fa416d9da3bd8f70d33ebb85d39` and froze deterministic offline
encode/decode comparisons. The assets retain the upstream Apache-2.0
provenance and require no network access after checkout.

## Pinned files

| File | Bytes | SHA-256 |
| --- | ---: | --- |
| `tokenizer.json` | 11,422,654 | `aeb13307a71acd8fe81861d94ad54ab689df773318809eed3cbe794b4492dae4` |
| `tokenizer_config.json` | 9,732 | `d5d09f07b48c3086c508b30d1c9114bd1189145b74e982a265350c923acd8101` |
| `vocab.json` | 2,776,833 | `ca10d7e9fb3ed18575dd1e277a2579c16d108e32f27439684afa0e10b1440910` |
| `merges.txt` | 1,671,853 | `8831e4f1a044471340f7c0a83d7bd71306a5b867e95fd870f74d0c5308a904d5` |

The canonical offline artifact is exactly 15,881,072 bytes. Including its
versioned manifest, frozen reference fixture, and verification adapter, the
tracked tokenizer test bundle is 15,896,037 bytes.

## Vocabulary and special tokens

| Property | Value |
| --- | ---: |
| Tokenizer class | `Qwen2Tokenizer` |
| Algorithm | NFC-normalized byte-level BPE |
| Base vocabulary | 151,643 |
| Merge rules | 151,387 |
| Added tokens | 26 |
| Total tokenizer entries | 151,669 |
| Model vocabulary size | 151,936 |
| Model IDs not assigned by tokenizer | 267 |

Tokenizer BOS and UNK are absent. EOS is `<|im_end|>` ID 151,645 and PAD is
`<|endoftext|>` ID 151,643. Model configuration separately declares BOS
151,643 and EOS 151,645.

Special added tokens are IDs 151,643-151,656:
`<|endoftext|>`, `<|im_start|>`, `<|im_end|>`, object-reference, box, quad,
vision-start/end/pad, image-pad, and video-pad markers.

Recognized non-special added tokens are IDs 151,657-151,668:
`<tool_call>`, `</tool_call>`, FIM prefix/middle/suffix/pad, repository/file
markers, tool-response markers, and think markers. The artifact manifest lists
every exact ID, spelling, and special flag.

## Separate limits

| Concept | Value |
| --- | ---: |
| Model maximum positions | 40,960 |
| Tokenizer-declared maximum length | 131,072 |
| Runtime session capacity | Caller-configured; no universal value |

No one limit is substituted for another.

## Chat-template metadata

The complete upstream template is preserved in `tokenizer_config.json` without
rendering support. It contains 4,168 characters/UTF-8 bytes and has SHA-256
`a55ee1b1660128b7098723e0abcd92caa0788061051c62d51cbe87d9cf1974d8`.
No chat framework, tool execution, or API surface was added.

## Reference comparisons

The offline adapter used Transformers 5.12.1 and tokenizers 0.22.2 with no
network access and `add_special_tokens=false`.

| Case | Token count | Exact IDs | Exact decode | Exact round trip |
| --- | ---: | --- | --- | --- |
| English | 9 | Yes | Yes | Yes |
| Thai | 11 | Yes | Yes | Yes |
| Source code and indentation | 10 | Yes | Yes | Yes |
| Whitespace and newlines | 8 | Yes | Yes | Yes |
| Unicode and emoji | 13 | Yes | Yes | Yes |
| Special tokens | 6 | Yes | Yes | Yes |
| Empty input | 0 | Yes | Yes | Yes |

The exact strings and expected ID arrays are in
`models/qwen3-30b-a3b/tokenizer-reference-v1.json`.

## Dependency decision

No Cargo dependency, Python dependency, native library, unsafe boundary, or
public Rust API was added. The adapter uses the existing locked Python oracle
environment. Implementing the upstream Unicode regex and BPE as a production
Rust text API remains deferred and requires its own review.

## Reproduction

```powershell
python python\reference\verify_tokenizer.py
```

The script forces Hugging Face offline mode, verifies source hashes before
loading, and uses `local_files_only=True`.

## Verification

Offline tokenizer verification passes all seven exact-match cases.
`cargo fmt --all --check`, `cargo check --workspace`,
`cargo test --workspace`,
`cargo clippy --workspace --all-targets -- -D warnings`, and
`cargo run -p clr-cli` all pass. The workspace suite contains 118 tests with
zero failures or ignored tests; Clippy emits no warnings and the CLI reports
`bootstrap ready`.

## Limitations

- Rust inference continues to accept token IDs rather than text.
- Chat-template metadata is preserved but not rendered.
- No weights, quantization, mmap, SIMD, GPU support, optimization, or
  full-model inference are part of this task.
